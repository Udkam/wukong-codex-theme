# 当前本地工作状态

运行时：v99-settings-background-continuity。按用户要求同步主题源码到远程，暂不打包发行；独立宠物修改保持原状。

- 浅色对话恢复明亮的白色薄遮罩，按场景调整；深浅色新对话均无遮罩。
- 设置往返保留背景模式；常驻健康检查不再因输入框临时未标记而重建整个运行时。
- 当前常驻注入器已更换为磁盘代码，避免旧 v98 重新覆盖修复。
- 10 张战斗图与 3 张风景图，风景默认雪山；用户手动选择和锁定保留。
- 唯一主题启动入口为 start.cmd，四个旧 .cmd 已删除；原生 ChatGPT.exe 启动行为不变。
- 76 个弃用存档按用户最新决定保留，清单见 CLEANUP.md。
- 本轮背景、原生绘制边界、事件注入器和生命周期定向测试共 77 项通过。

用户本轮实测反馈“没有问题了”。诊断确认设置内深浅切换及返回期间，运行时与背景层对象始终未替换。临时诊断记录器已移除。

本轮最终 npm run check：161 项，154 通过、0 失败、7 跳过。默认配置已同步当前 13 张图和雪山优先顺序；开始菜单已有主题快捷方式已指向 start.cmd。未执行发布或删除历史存档。
